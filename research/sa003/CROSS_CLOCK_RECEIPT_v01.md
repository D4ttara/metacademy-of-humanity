# Cross-Clock Receipt

Fixture `CC-01` records local event order `A → B → C` under clock `alpha` and `A → C` under clock `beta`. Translation preserves the partial causal relation `A before C` while refusing to invent a global duration. Result: `PASS / PARTIAL_ORDER_PRESERVED`.

This is an engineering receipt from deterministic synthetic data. It does not establish a physical theory of time.
