# M{Y}OGA JYOTISH · Method Freeze v0.10

**Status:** VERIFIED EXECUTABLE RESEARCH FRONTIER · NO EFFICACY CLAIM

v0.10 freezes the first narrow non-null M{Y}OGA contestant and a deterministic non-astrological null baseline **before a new unseen public case is selected**.

## Blind task

The future case exposes a verified birth packet plus event dates/windows, while event descriptions and event domains remain hidden until reveal.

`EVENT DATE VISIBLE != EVENT SEMANTICS VISIBLE`

## Frozen primary contestant

`MYOGA_VIMSHOTTARI_DOMAIN_CONVERGENCE_V010`

For each visible event date the method computes active Vimśottarī MD + AD, evaluates the frozen domain vargas, counts `SAME_SIGN`, `LAGNA_CONTACT`, and `DISPOSITOR_LINK`, and predicts every domain tied at the maximum non-zero structural score. A zero maximum returns `UNRESOLVED`.

The supported research router is:

- career/status/public manifestation → D10
- partnership/spouse → D9
- children/descendants → D7
- education/knowledge → D24
- practice/devotion → D20
- adversity/crisis → D30

BPHS domain anchors and Vimśottarī are classical source inputs; the combined classifier is explicitly `M{Y}OGA_EXTENSION`, not a rule attributed directly to BPHS.

## Frozen null

`NULL_DATE_HASH_DOMAIN_BASELINE_V010`

The null sees the same visible event date and selects one of the six domains by deterministic SHA-256 mapping. It contains no astrology and makes no randomness-quality claim.

## Freeze receipt

Immutable method-freeze SHA-256:

`307780dc21d009e1f5a05757472e943caf1422710dac800bc368eba6436ca8fc`

Final source package SHA-256:

`2d12c7e3626f99b30e64ac317eaa180cd47c9a4747d1ed08f5926b0aae6f0ed5`

Packaged clean-reextract regression:

`160 JS + 3 base Python + 80 MSL/M{Y}OGA/DotLinux Python + DOTVM C`, final `EXIT 0`.

## Migration state

The Python Astroprocessor research chain now has canonical implementation ownership under `myoga.*`. Historical `samyoga.*` import paths are compatibility shims where retained; historical SamYoga publication artifacts remain unchanged.

## Guards

`METHOD RULES FROZEN BEFORE UNSEEN CASE SELECTION`  
`METHOD MUTATION REQUIRES NEW VERSION`  
`CLASSICAL DOMAIN ANCHOR != CLASSICAL COMBINED PREDICTION RULE`  
`SCORE != PROBABILITY`  
`NULL BASELINE != ASTROLOGICAL METHOD`  
`FREEZE != EFFICACY CLAIM`  
`MIGRATION != DELETION`

© 2026 Ievgen Karogod / Dattara · MET[Ȧ]CADEMY OF HUMANITY (MoH)
